# CLAUDE.md — GitPeek

個人用 Git ビューワー **GitPeek**（Tauri v2 + Rust + React/TypeScript）。

本ファイルは**実装中に毎回効く制約**だけを抜き出したもの。

| ファイル | 担当 |
|---|---|
| 本ファイル | **何を守るか** — 破ると静かに壊れる制約 |
| [`task_lists.md`](task_lists.md) | **今やること** — タスクの詳細と進捗（進捗の唯一の正） |
| [`docs/DESIGN.md`](docs/DESIGN.md) | **なぜそう決めたか** — 全決定と決定理由 |

**新しいセッションは `task_lists.md` の「現在地」節から読むこと。**

---

## 1. このアプリは何をしないか

GitPeek は**ビューワー**である。以下は「未実装」ではなく「実装しないと決めた」もの。
これらを追加する提案・実装をしてはいけない。

- あらゆるコミット操作（commit / amend / rebase / cherry-pick / revert）
- stage / unstage / discard / stash の作成・適用（作業ツリーは **read-only** 表示のみ）
- `--force` 付き checkout、自動 stash
- 非 fast-forward マージ（`merge` は常に `--ff-only`）
- shallow clone (`--depth`)、`--single-branch`、clone 時のブランチ指定 (`--branch`)
- **サブモジュールの自動取り込み。** `--recurse-submodules` を付けるのは
  「clone の確認画面で**利用者がチェックした**とき」だけ
  （2026-09-05 に利用者の判断で追加。DESIGN.md §8.4）。**既定は OFF で、黙って付けない。**
  `--shallow-submodules` は付けない（そのフォルダを別途登録しても読めなくなる）
- **タグ**の作成・削除・リネーム、**ブランチの削除・リネーム**
- **ブランチの自動作成。** ref を新しく作るのは
  「リモート追跡ブランチを checkout するときに、**利用者が確認画面で選んだ**ローカル追跡ブランチ」
  1 つだけ（2026-09-04 に利用者の判断で追加。DESIGN.md §8.1）。
  黙って作ってはいけない。選ばれなければ detached HEAD にする
- 外部通信（Gravatar 等）。ネットワークに出るのは git の fetch/clone と LLM API だけ
- 自動更新、コード署名

git に対して書き込むのは **checkout / fetch / merge --ff-only / clone** の 4 つだけ。

### この一覧を変えるときは利用者に聞く

**エージェントの判断でここを緩めてはいけない。** 必要だと思ったら、実装せずに利用者へ聞くこと。

これまでに緩めたのは 2 件で、**どちらも利用者が決めた**。緩め方も揃えてある —
**禁止をやめたのではなく、「黙ってやらない」ほうを残して条件を付けた。**
同じ形が必要になったら、この 2 件に倣うこと。

| いつ | 何を | 付けた条件 |
|---|---|---|
| 2026-09-04 | ローカル追跡ブランチの作成（`checkout -b --track`）| checkout の確認画面で**選ばれたときだけ**（DESIGN.md §8.1）|
| 2026-09-05 | サブモジュールの取り込み（`--recurse-submodules`）| clone の確認画面で**チェックされたときだけ**。既定 OFF（DESIGN.md §8.4）|

---

## 2. git 実行の不変条件（破ると静かに壊れる）

**すべての git 実行は `src-tauri/src/git/exec.rs` を通す。**
ここ以外で `Command::new("git")` を書かないこと。

入口は 3 つある。

| 入口 | 用途 |
|---|---|
| `run` | 完了を待つ。ほとんどはこれ |
| `run_streaming` | **stdout** を読みながら途中経過を返す（全件ダンプ） |
| `run_progress` | **stderr** を読みながら返す ＋ 中止できる（fetch / clone の進捗） |

**3 つとも `build()` で固定オプションと固定環境変数を付ける。** 一部だけに足すと、
経路によって日本語パスが化けたり認証で固まったりする。新しい入口を増やすときも
`build()` を通すこと。

### 固定オプション（全呼び出しに必ず付ける）

```
-c core.quotepath=false    # 無いと日本語ファイル名が 8 進エスケープされる（この環境は未設定＝true）
-c core.autocrlf=false     # ユーザーの .gitconfig に左右されない挙動を得る
-c core.pager=cat          # pager 起動でハングするのを防ぐ
-c color.ui=false          # ANSI エスケープの混入を防ぐ
-c core.commitGraph=false  # 全件ダンプでは commit-graph があると 3 倍遅い（DESIGN.md §3.1）
```

### 固定環境変数（全呼び出しに必ず設定）

```
GIT_TERMINAL_PROMPT=0                    # 無いと認証時に GUI 子プロセスが無言でハングする
GIT_SSH_COMMAND=ssh -o BatchMode=yes     # 無いとパスフレーズ待ちでハングする
```

### その他

- **`git log --all` を使ってはいけない。** `--all` は `refs/tags/`・`refs/stash`・`refs/notes/` を
  含み、「タグを起点 ref にしない」という決定に違反する。正しくは
  **`git log --branches --remotes HEAD --topo-order`**。
- **`.git/index.lock` をアプリから削除しない。** 検出したら表示するだけ。
- 認証は git CLI（Git Credential Manager）に完全委譲する。アプリはパスワードもトークンも扱わない。
- 出力パースは機械可読形式（`--format` / `-z` / `--porcelain=v2` / `--numstat`）のみに依存する。
  人間向け出力をパースしない。
- **`--raw` / `--numstat` の `-z` 出力では、リネームとコピーだけパスを 2 つ食う。**
  1 つとして数えると以降のファイルが全部 1 件ずつずれる（`git/diff.rs`）。
- **ルートコミットに `git diff` を使わない。** 片側を指定できないので
  `diff-tree --root --no-commit-id -r` を使う（patch が要るときは `-p` も）。
  空ツリーの SHA を定数で埋め込まないこと（SHA-256 リポジトリで値が変わる）。
- **リネームしたファイルの差分は pathspec に古いパスと新しいパスの両方を渡す。**
  新しいパスだけだと git はリネームを検出できず、**全行が追加された新規ファイル**として出す。
- ahead/behind は `git rev-list --count` を呼ばず、**メモリ上のグラフから計算**する。
- **`%s` は最初の段落を 1 行に潰す。** 要約が複数行にまたがるコミットで改行が空白になるので、
  **メッセージ全文が要るところは `%B` を取り直す**（`%s` + `%b` から組み立て直さない）。
- **clone だけは `-C` を使わない。** まだ無いフォルダを作る操作なので、`repo` は `None` にし、
  **URL と保存先を絶対パスで渡す**（DESIGN.md §8.4）。相対パスを通すと、利用者の知らない
  場所（アプリの作業ディレクトリ）に作られる。
- **clone の残骸を消してよいのは「実行の直前に無かったと確かめられた」ときだけ。**
  確かめられなければ消さずに場所を伝える。利用者の既存フォルダを消す事故は、
  条件分岐ではなくこの構造で潰してある（DESIGN.md §8.4）。
- **checkout の対象は形で渡し分ける**（実測。DESIGN.md §8.1）。
  **ローカルブランチは短い名前（`main`）**、それ以外は**完全な ref 名 ＋ `--detach`**
  （`refs/remotes/origin/main` / `refs/tags/v1` / SHA）。
  短い名前だけを渡すと、ローカルに同名が無いとき git の DWIM が
  **リモート追跡ブランチを追跡するローカルブランチを勝手に作る**（§1 違反）。
  逆にローカルブランチへ完全な ref 名を渡すと、ブランチとして扱われず detached になる。
  ローカル追跡ブランチを作るのは `checkout -b <名前> --track <完全な ref 名>` だけで、
  **確認画面で選ばれたときにしか通らない経路**にしておくこと（§1）。
  **`--detach` で塞ごうとしないこと** — DWIM が内部で `-b` を立てるため、
  `'--detach' cannot be used with '-b/-B/--orphan'` という見当違いのエラーで落ちる。

---

## 3. レーン割り当ての不変条件

`src-tauri/src/graph/lane.rs`。**このアプリの心臓部。変更したら必ずユニットテストを通すこと。**

1. **lane 0 は幹に予約される。** 既定ブランチ（`origin/HEAD` → `main`/`master` → HEAD の順で決定）の
   第一親チェーンは必ず lane 0 を通り、一直線に描かれる。
   **幹は既定ブランチより新しい側へも伸ばす**（その先端を第一親に持つ子を辿る）。
   既定ブランチより先へ進んだブランチを開いていると、そこで幹が途切れて別レーンへ折れるため。
2. レーン再利用は最小空きレーンだが、**解放直後の 2 行は再利用を保留**する
   （`RESERVE_ROWS`。1〜4 のどれでも実測は変わらなかった — DESIGN.md §15.1）。
   **ルートコミット（親なし）で終わったレーンは二度と配らない。** 他の終わり方は線が
   別レーンへ曲がって続くので終わりが見えるが、ルートだけは線がその場で止まるため、
   数行後に同じレーンを別の枝が使うと同じ色の線が穴を空けて続いて見える。
3. マージコミットの第 2 親は**右側に新レーンを起こす**。
4. **レーン数に上限を設けない**（グラフ列を横スクロールさせる）。
5. コミットは**リポジトリ選択時に全件メタ情報を一括取得**し、レーンを一度で確定する。
   増分読み・増分レーン計算はしない（描画中にグラフが踊るため）。
6. 作業ツリーの擬似行は**レーン計算の対象外**。HEAD へ点線で接続する。
7. ブランチ表示 ON/OFF は、可視 ref 集合から**到達可能集合を再計算してレーンを振り直す**
   （淡色化ではない）。

---

## 4. セキュリティ上の必須事項

- **秘匿情報のマスキングは `src-tauri/src/redact.rs` を全出力が通ること。**
  画面表示とログファイルの両方で、`://<user>:<secret>@` パターンと API キーらしき文字列を
  マスクしてから記録する。`git remote -v` や git の stderr に平文トークンが出るため。
  **通す場所は「その文字列を組み立てるところ」で 1 度だけ**にする（呼び出し側に任せない）。
  どこが通っているかの一覧は DESIGN.md §13.4.1。
- **git の stderr は `git/exec.rs` が `GitOutput` を組み立てるところで通してある。**
  ここを通さずに stderr を組み立てる経路（新しい入口）を作らないこと。
  **git 自身が伏せることを当てにしない** — 伏せる経路と伏せない経路の両方がある。
  試すときは**伏せない形**（知らないオプションを渡すと受け取った文字列がそのまま出る）で
  書く。伏せてくれる形（`fetch <資格情報付き URL>`）で書くと、
  **マスクを外してもテストが緑のまま通る**。
- **ログファイルへ書くものは `logging::format_line` を通る。** `log::info!` などの
  `log` の口から書けば自動的に通るので、**ファイルへ直接書く経路を作らないこと。**
- **ログに本文を書かない。** プロンプト・差分・レビュー本文・skill 本文・ファイル内容は
  対象外（`%APPDATA%` に他人のソースと秘密が残る）。LLM は接続先・モデル・所要時間・
  結果の種別まで。**git の stderr は全文残す**（2026-09-06 に利用者が決めた）。
- **API キーの平文に触ってよいのは `src-tauri/src/secret.rs` と `src-tauri/src/llm/client.rs` だけ。**
  `settings.json` に書くのは参照キー（`credential_key`）だけ。**通信は保存済みのプロファイルに
  限る** — フロントから平文のキーを受け取って試せるようにすると、保存を経ない経路が増える
  （DESIGN.md §10.2.1）。押せない理由は画面に出すこと。
- **参照キーを決めるのは `Store::upsert_llm_profile` だけ。** フロントから届いた
  `credential_key` を使わない（別のプロファイルの資格情報を指す形が作れてしまう）。
  **プロファイルを消したら資格情報も消す**（孤児を残さない）。
- **API キー欄は「空欄＝変えない」。** 空欄を「消す」にすると、名前だけ直して保存したときに
  キーが消える。`keep` / `replace` / `clear` を**真偽値ではなく種類**で送る。
- **LLM から外へ出る文字列は `client.rs` の `sanitize` を通す**（`redact` ＋ 使ったキー自体の
  伏せ字化）。規則に当たらない形のキーでも結果に出さないため。
  **キー自体の伏せ字化は「独立した語」のときだけ行う**（`mask_key`）。素の `replace` にすると、
  **1 文字のキー（`a`）で応答が壊れる** — `message` が `mess***ge` になり、JSON のキー名まで
  潰れて理由を取り出せなくなる（2026-09-05 の目視で実際に起きた）。
- **リポジトリ内 skill (`<repo>/.gitviewer/skills/*.md`) は既定で無効。**
  他人のリポジトリを開く用途がある以上、リポジトリ内の指示文でレビュー結果を操作されうる。
  **信頼は「このスキルを使う」でファイル 1 つずつ**（`llm/skill.rs`）。フォルダ単位に
  すると「無害な 1 つで信頼させてから 2 つ目を置く」攻撃に特別扱いが要るが、
  ファイル単位なら**増えたものは記録が無いので未決のまま**で済む。内容が変わったら
  **そのファイルだけ**が未決へ戻る。削除は記録が残るだけ。
- **押した時点で読んでいた内容だけを信頼する。** 「使う」と決めるときは、画面が見せていた
  ハッシュを一緒に受け取り、実物と食い違ったら記録しない（表示してから押すまでの間に
  書き換えられる隙を残さない）。
- **プロンプトへ渡せる skill 本文が出てくるのは `SkillEntry::usable_body` だけ。**
  本文のフィールドは非公開で、信頼していないものはそこへ入らない。**既定は「配らない」**で、
  信頼を確かめたときにだけ渡す（配り忘れは動かないだけだが、止め忘れは漏れる）。
  画面で読ませる本文は `preview` に分けてある。**こちらを LLM へ渡してはいけない。**
- **skill は直下の `*.md` だけ読む。サブディレクトリと symlink を辿らない**
  （`.md` の名前で秘密鍵を指されると中身がプロンプトへ入る）。1 ファイル 64 KiB /
  1 か所 64 件を上限にする。
- **使わない skill が、同名の別の skill を押しのけてはいけない。**
  押しのけると、決めていないだけで何も使えなくなる。**どちらが効いているかは画面に出す。**
- **skill ごとの「追加の指示」は `settings.json` に持ち、skill ファイルを書き換えない。**
  他人のリポジトリの skill にも足せるし、信頼のハッシュも壊れない。
  本文の後ろに付いた形で `usable_body()` から出るので、**未信頼の skill には付かない。**

---

## 5. データと設定の置き場所

```
%APPDATA%\com.tatsu.gitpeek\
├── settings.json          # 手編集を想定。schemaVersion 必須
├── state.json             # アプリが随時上書き。壊れたら捨てて再生成できること
├── skills\                # グローバル skill (*.md)
├── reviews\<repo-id>\     # レビュー結果 (<日時>-<runId 8 桁>.json)。上書きせず履歴として積む
└── logs\                  # gitpeek-YYYY-MM-DD.log（7 日ローテーション）
```

**OneDrive 配下に設定を置かない**（同期競合とファイルロックの温床）。

- **`settings.json` の数値の範囲は 2 か所で締める。** 画面（`src/lib/settingsForm.ts` の
  `NUMBER_FIELDS`）と Rust（`Settings::normalize()`）に**同じ値**を置くこと。
  手編集を想定しているので画面だけでは足りず、Rust だけだと打ち間違いが黙って
  書き換えられる（画面には理由が出ない）。**片方だけ変えると Rust のテストが落ちる。**

- **レビュー結果を保存するのは `start_review` の中**（`store/reviews.rs`）。
  フロントに保存させると、例外や画面遷移で**保存し忘れる経路**ができる。
  **中止したものも積む**（途中まででも読む価値がある。一覧に「中止」と出す）。
  ファイル名に `runId` を足すのは、**同じ秒に 2 件で衝突させない**ため
- **保存するのは `ReviewRun` をそのまま包んだもの。** 別の形へ変換しない
  （変換器を両側に合わせ続けることになる）。包む側が `schemaVersion` /
  `repositoryId` / `savedAt` / プロファイルの控えを足す（DESIGN.md §12.4）
- **プロファイルの `baseUrl` は `redact` を通してから書く**（§4）
- **壊れた 1 件で一覧を落とさない。** 読めないものと将来版は**理由付きで一覧に残す**

---

## 6. UI の固定事項

- 日本語 UI。**全表示文言は `src/i18n/ja.ts` に集約**し、コンポーネントからはキーで参照する
  （i18n ライブラリは入れない）。文言をコンポーネントに直書きしないこと。
  **唯一の例外が `index.html` の起動時受け皿**（DESIGN.md §13.5）。`ja.ts` を読めない状況を
  扱うためのものなので、ここへ移してはいけない。
- **リポジトリに紐づく設定を、アプリ全体の「設定」に混ぜない。** どのリポジトリの話を
  しているのか読めなくなる（2026-09-05 に利用者の指摘）。リポジトリ 1 つぶんの設定は
  **リポジトリ一覧の右クリック →「このリポジトリの設定」**から開く。
  アプリ全体の「設定」に残るのは、内蔵と自分で置いたもの（グローバル）だけ。
- レイアウト: **3 ペイン**。左サイドバー（リポジトリ一覧 / ブランチ・タグツリー）
  ＋ 中央上グラフ・中央下差分本体 ＋ 右にコミット詳細と変更ファイル一覧
  ＋ さらに右の AI レビュードロワー。
  **コミット詳細と変更ファイル一覧を差分本体の上に積まない**（DESIGN.md §6.1）。
  積むと詳細と一覧が高さを食って差分に数行しか残らない。
- **ダイアログの中のボタンは折り返す。** 共通の `.button` は `white-space: nowrap`
  （狭いペインでラベルを割らないため）。ラベルに**ブランチ名が入る**場所でそのままにすると、
  長い名前でボタンが箱の外へ出る（実測: ボタン 507px / 箱 480px、左へ 52px はみ出す）。
  `.modal__actions .button` で `white-space: normal` ＋ `overflow-wrap: anywhere` に戻す。
- **`.button--primary` に共通の `.button:hover` を効かせない。** 中立の `--bg-hover` が
  accent を上書きし、**文字が地に溶けて「押せない」ように見える**（輝度比 1.20 / 1.30 を実測）。
  primary 系のボタンを足すときは `--accent-hover` を使う。
- テーマは OS 追従が既定。**色はライト / ダークで別定義する。明るさも別に決める**
  （白地に濃い色を置くとグラフ全体が暗く沈む — DESIGN.md §5.2）。
  レーン色の並び順は隣接レーンの色差で決めてあるので、入れ替えるときは測ること。
- グラフ寸法: 行高 28px、レーン幅 14px、ノード列の左マージン 12px。
- **コミットリストの列見出しは行と同じスクロールコンテナに入れ、縦だけ sticky で貼る。**
  外に出すと横スクロールで行とずれ、親ペインに 2 本目のスクロールバーが生える。
  見出しの高さ（`--head-height`）は仮想スクロールの `scrollMargin` と同じ値にすること。
- **グラフの SVG は行の背景より前面に置く**（`pointer-events: none`）。背面だと
  ホバーや選択の背景に線が塗り潰される。
- **行の中に置く SVG は `display: block` にする。** 既定の `inline` だとベースラインの下に
  余白が付き、**ノードが 4px 上へずれる**（作業ツリーの擬似行で実測）。
- **コミットリストに非コミット行を足すときは、グラフの窓の置き場所だけを下げる。**
  グラフは行番号そのままの座標で描いているので、viewBox にも足すと打ち消し合って
  1 行ぶん上へずれる。
- **コミットリストの行は `LaneLayout.rows` を順に辿って作る。** 読み込んだコミット配列の
  添字で引いてはいけない。レーンは可視 ref で絞られ、date 表示では組み替わるので、
  どちらの経路でも行の文字と線が 1 行ずつずれる。
- **中身が伸びるスクロールペインの `flex-basis` は 0 にする**（`flex: 1 1 0`）。`auto` だと
  中身の高さが基準サイズになり、縮小が基準サイズの比で配分される。隣のペインは
  200px 対 数万 px の比でほぼ全部を削られて数 px に潰れる（ref 数千本のリポジトリで発生）。
- **ダイアログの選択肢は、押せないときも消さない。** 押せる条件でだけ出すと、
  条件が常に成立しない場面でその選択肢が画面から消え、**何が効いているのか読めなくなる**
  （clone の「この保存先を次回から既定にする」で実際に起きた）。押せない形で残し、
  押せない理由といまの値を文言に出す。**判定はコンポーネントに直書きせず純関数へ出す**
  （§8 — 直書きはテストが 1 つも当たらない）。
- **文言は「何が起きるか」で書く。** git の用語をそのまま出さない
  （「FF マージ」は通じなかった）。メニューは「main に origin/x を取り込む」のように
  **方向を名前で書き**、**できないときは何ができないのかを説明する**。
  チェックボックスはラベルの下に結果を 1 行添える。
- **AI レビューのドロワーはコミット情報の列と入れ替える**（並べない。DESIGN.md §6.1）。
  3 列 ＋ ドロワーでは差分が潰れる。幅は `paneRatios.reviewDrawerWidth` に別で持ち、
  畳んだ列の幅と混ぜない。**実行前パネルはモーダルにしない**（差分を見ながら
  外すファイルを決められなくなる）。
- **AI レビューの指摘のバッジは、差分に実際にある行にだけ出す。** モデルは差分に
  無い行番号をよく書くので、無い行に出すと**別の行の指摘に見える**。当たらなかった
  ものは**ドロワーに残して当たらなかったと明記する**（消さない）。
  振り分けは純関数（`lib/reviewFindings.ts`）。
- **タグにチェックボックスを置かない。** タグは起点 ref ではないので外しても
  グラフが変わらない（DESIGN.md §4.4）。`visibleRefs.excluded` にタグの ref 名を入れないこと。
- **アプリ全体の設定の入口は 1 つ**（ヘッダの「設定」と `Ctrl+,`）。テーマも接続先も
  その中にある。**リポジトリに紐づくものは右クリックの「このリポジトリの設定」**（上記）。
  **覚えている既定は必ず画面に出す** — 読めないと、勝手に変わっているようにしか見えない。
- **キーの形は `src/lib/shortcuts.ts` の対応表だけが持つ。** 判定は `matches(event, action)`
  を通し、`.tsx` やフックに `event.key === ...` を書かない。設定画面の一覧も同じ表から
  作るので、**書き足すと一覧にも出る**（一覧が飾りにならない）。
  **修飾キーは「書いていないものは押されていない」ことまで見る**（`Ctrl+Shift+R` が
  `Ctrl+R` に当たらないように）。`Esc` は `hooks/useEscape.ts`。
- **差分の中の検索（`Ctrl+F`）は文字を塗らない。** 当たった行へ移動して囲むだけにする。
  行には既に語単位差分と構文色が載っていて、3 つ目の色を重ねると**どれが変更で
  どれが検索結果か読めなくなる**。
- **git コマンドログパネルを常設**（直近 500 件、リポジトリ切替でクリアしない）。
  実行コマンド・exit code・stderr・所要時間を表示。エラーは「人間向けメッセージ＋展開で生 stderr」。

---

## 7. AI レビューの固定事項

- **OpenAI 互換 API 1 系統のみ**。Ollama も `http://localhost:11434/v1` で扱う。
  **LLM への HTTP は `src-tauri/src/llm/client.rs` を通す**（タイムアウトと `Authorization` の
  付け外しがここに固定してある）。`Authorization` は**キーが空でないときだけ**付ける
  （Ollama は認証不要）。base URL は末尾の `/` を落としてから繋ぐ。
- **LLM の失敗は「見出し ＋ 接続先の言い分 ＋ 展開で生の応答」の 3 段で出す。**
  ステータスだけでは原因に辿り着けないので、**サーバ自身の説明を見出しへ引き上げる**
  （`server_message`。1 行に潰して 200 文字で切る）。生の応答の整形は
  `src/lib/llmMessage.ts` の純関数で、**JSON として読めたときだけ**行う。
- **接続テストは `/v1/models` で済ませない。** models が通っても生成まで通るとは限らないので、
  `chat/completions` に `max_tokens` 小さめで 1 往復させる。**タイムアウトは接続 10 秒 /
  応答 120 秒**（ローカル Ollama は初回のモデルロードで数十秒かかる）。
- **レビューの実行は `src-tauri/src/llm/review.rs` を通す。** 計画（見積もりと分割数）を
  決めるのはここだけで、**同じ計算をフロントに置かない**（必ずずれる）。
  **差分本文は `FileDiff` の hunks から組み立てる** — `git diff` の生出力を別経路で
  取り直すと、文字コードの判別を通らず **Shift_JIS のファイルで JSON が壊れる**。
- **ストリーミングも `client.rs` に閉じる**（`chat_stream`）。SSE のパースも中止の見張りも
  ここで、`review.rs` から `ureq` を直に触らない。待ち時間は**別立て**にすること
  （`timeout_global` のままだと長い生成を途中で切る）。
- **流す差分は末尾を持ち越してからマスクする**（`Masker`）。チャンクの切れ目で秘匿情報が
  割れると、チャンクごとの `sanitize` を素通りする。**幅は 2 つと突き合わせて決める** —
  伏せたい形が丸ごと収まること（`sk-` ＋ 40 字程度）と、**実際に来る応答より短いこと**。
  256 文字にしていたら、それより短い応答で**差分が 1 度も流れなかった**（2026-09-06 の目視）。
  **鍵が幅より長いときは鍵の長さまで広げる**（固定幅だと割れた鍵が素通りする）。
- **`response_format: {"type":"json_object"}` は送る**（Ollama は `format: "json"` に読み替える）。
  **咎められたら外して 1 度だけ投げ直し、以後その実行では付けない**（DESIGN.md §10.7.1）。
  **プロンプトでの JSON 要求は外さない。**
- **400 の投げ直しは 1 度だけ、かつ見立てが立つときだけ。** 振り分けは `client.rs` の
  `retry_hint`（`response_format` を咎めている / 文脈長を咎めている / どちらでもない）。
- **コミットメッセージを渡すのは「1 つのコミットとその親」のときだけ。**
  2 点比較と作業ツリーでは渡さない。同じ形なので**親子かどうかは git に聞く**
  （`diff::is_parent_of`）。
- 投入単位は**ファイル単位＋最後に全体サマリ**。超過時のみ hunk 分割にフォールバック。
- モデルに渡すのは **パス ＋ unified diff (`-U10`) ＋ コミットメッセージ ＋ skill 本文**のみ。
  **ファイル全文は渡さない。** skill 本文は **`SkillEntry::usable_body()` からしか取らない**
  （§4）。`preview` は画面で読ませるためのもので、プロンプトへ入れてはいけない。
  絞り込みは `SkillEntry::active()`（隠されていない ＋ 使う ＋ 信頼済み）を通す。
- **JSON スキーマを要求し、パース失敗時は生出力を Markdown として表示するフォールバックが必須。**
  この経路はテストで必ず通すこと（ローカル小型モデルはスキーマを守れないことがある）。
- 実行は既定逐次（並列度 1、最大 3）。1 ファイルの失敗で全体を止めない。

---

## 8. テストで必ず担保すること

- **レーン割り当て**: 直線 / 単純分岐 / 連続マージ / オクトパスマージ / ルートコミット複数 /
  lane 0 予約 / レーン再利用の保留 / ルートで終わったレーンを配らないこと /
  可視 ref を絞ったときの再計算
- **到達可能性**: ahead/behind が `git rev-list --count` と一致すること
  （**テストコードでだけ git を呼んでよい**）／ ブランチを 1 本外すと該当コミットだけが消えること
- **git 出力パーサ**: 日本語ファイル名 / 空 subject / 複数親 / 署名付きコミット / 改行を含む subject
- **変更ファイル一覧**: 追加 / 変更 / 削除 / **リネーム（前後がずれないこと）** /
  バイナリ（増減が 0 ではなく「無い」こと）/ サブディレクトリ / ルートコミット /
  マージコミットの親切替
- **文字コードと改行**: UTF-8 / BOM 付き UTF-8 / Shift_JIS / EUC-JP / どれでもないバイト列 /
  NUL 入り（バイナリ）／ **CRLF を CR と LF に二重計上しないこと** / 混在の検出
- **差分パーサ**: 追加のみ / 削除のみ / 複数 hunk / 個数の省略された hunk ヘッダ /
  **`\ No newline at end of file` を行として数えないこと** / モード変更 / シンボリックリンク /
  バイナリ / 内容の変わらないリネーム / **ヘッダ行（`--- ` `+++ `）を内容行にしないこと**
- **差分の表示**（純関数）: side-by-side の行対応（数が違うとき余りが空欄になること）/
  語単位差分（先頭・末尾・両方・共通が短いとき）
- **作業ツリーの状態**: クリーン / ステージ済み / 未ステージ / 未追跡 / **衝突（同じパスが
  `diff --raw` に 2 度出ること）** / 同じファイルがステージ済みと未ステージの両方に出る場合
- **fetch の進捗パース**: **`\r` 区切り**（`\n` だけで切ると 1 行も確定しない）/ `remote:` の前置 /
  割合の無い行 / **翻訳された進捗**（ラベルで判定していないこと）/ 進捗でない行 / 壊れた行 /
  **UTF-8 の途中で切れた断片**
- **fetch の結果**: リモート追跡 ref が増えること / `--prune` で消えた ref が消えること /
  **リモートが無いリポジトリで放置警告が立たない**こと / **上流のタグ付け替えが「一部成功」**に
  なること / 資格情報のマスキング
- **checkout と FF マージ**: 引数の形（**ローカルブランチは短い名前 / それ以外は完全な ref 名
  ＋ `--detach`**）／ **リモート追跡ブランチを開いてもローカルブランチが増えないこと** /
  確認を経たときだけ作られること ＋ 上流が設定されること /
  判定（bare / 汚れている / `index.lock` / unborn で止まり、**未追跡だけなら止まらない**）/
  **分岐したブランチで FF マージが実行されず HEAD が動かないこと**
- **clone**: 引数の形（**`--depth` / `--single-branch` / `--branch` / `--shallow-submodules` が
  現れないこと**）／ **`--recurse-submodules` が既定で現れず、選ばれたときだけ現れること**
  （組み立てだけでなく、**実際に起動したプロセスの記録**でも見る）／
  URL → 既定のフォルダ名（`.git` 付き / 無し / 末尾スラッシュ /
  **SCP 形式 (`git@host:o/repo.git`)** / 決められない入力は空）／ 生成した bare からローカル
  clone できること ／ **既にあるフォルダなら git を起動する前に止まり、消さないこと** /
  **失敗しても自分が作らなかったフォルダを消さないこと** / 中止が「失敗」にならず残骸も残らないこと /
  資格情報付き URL が結果と**コマンドログの両方**でマスクされること
- **ダイアログの選択肢の出し分け**: **押せないときも消えないこと。**
  パスの比較は Rust 側の `same_path` と同じ（末尾の区切りと大文字小文字を吸収すること）
- **フロントから送る構造体**: **Rust 側が実際にその JSON を食えること。**
  引数の組み立てだけ固定しても、受け取りの形が違えばコマンドは 1 度も走らない
  （serde の `rename_all` は変種の名前しか変えない。フィールドは `rename_all_fields`）
- **資格情報**: 保存 → 読み出し → 削除の往復 ／ 削除後は `None` ／ **削除の再実行が失敗しない**
  こと ／ **`settings.json` に平文のキーが 1 文字も現れないこと**（ファイルを読んで確かめる）。
  **テストは本番と別のサービス名を使い、必ず後始末する**
- **LLM の接続**: 正常な `/v1/models` ／ 正常な chat ／ 401 ／ 404 ／ **JSON でない応答** ／
  接続不可 が**言い分けられる**こと ／ いずれでも**キーが結果に現れない**こと
  （サーバが echo してきても）／ `Authorization` が**キーの空のときに付かない**こと ／
  base URL の末尾スラッシュで**実際に飛ぶパスが変わらない**こと。
  **モックサーバへ実際に飛んだ要求で見る**（組み立ての固定だけでは足りない）／
  **1 文字のキーで応答本文が壊れないこと**（マスクが単語の途中を潰さないこと）
- **skill と信頼モデル**: **決めていない skill の本文がプロンプト側へ 1 文字も出ないこと**
  （本文に目印を入れて確かめる）／ 使うと決めたあと **①内容を変えると そのファイルだけ**
  未決へ戻り、**②増えたファイルは未決のまま**で、**③隣のファイルは巻き込まれない**こと ／
  消したファイルが残りを乱さないこと ／ **サブディレクトリと symlink を辿らない**こと ／
  上限を超えたものが読まれないこと ／ 同名のとき**使うと決めたほうが勝つ**こと ／
  frontmatter のパース（正常 / 無し / **空ファイル** / 閉じていない / 知らないキー /
  BOM / CRLF）／ **読めないものが理由付きで一覧に残ること** ／
  **「追加の指示」が本文の後ろに付き、未信頼の skill には付かないこと**
- **レビュー実行**: 正しい JSON / **フェンス付き JSON** / 前後に地の文 / 壊れた JSON /
  JSON でない Markdown / **途中で切れた応答** / **応答 0 バイト** のいずれでも
  **受け取った本文が残る**こと ／ 1 ファイルの失敗で全体が止まらず `failed` が数えられること ／
  **中止がストリームの途中でも効き、次のファイルへ進まない**こと ／
  **キーが結果に出ない**こと（**1 文字 / 空 / 長い**の 3 本 ＋ **チャンクに割れた場合**）／
  **未信頼 skill の本文が要求本文に 1 文字も出ない**こと（目印で確かめる）／
  当たらない `globs` の skill が載らないこと ／ **ファイル全文を渡していない**こと ／
  分割して投げた結果が 1 件にまとまること ／ **並列度どおりに走る**こと ／
  `response_format` を咎める 400 で**外して 1 度だけ**投げ直し、**以後付けない**こと ／
  **見立ての立たない 400 は投げ直さない**こと ／
  **`stream` を無視して普通の JSON を返すサーバ**を読めること
- **SSE のパース**: `data:` 複数 / `[DONE]` / 空行 / コメント行（`: ping`）/
  **JSON でない `data:` 行** / **チャンクの途中で切れた行** / CRLF / `delta` ではなく `message`
- **ストリーミングは「届いた回数」も数える。** 「流した差分を繋ぐと本文になる」だけだと、
  **最後にまとめて 1 回だけ届いても緑になる**（2026-09-06 の目視で踏んだ）。
  短い応答を細かく割って返し、**2 回以上に分かれて届くこと**を見る
- **レビュー結果の保存**: 同じ差分を 2 回で**2 件残る**こと（上書きしない）/
  同じ秒に 2 件でも名前が衝突しないこと / **壊れた 1 件で一覧が落ちない**こと /
  将来版が理由付きで残ること / **履歴 0 件で失敗しない**こと /
  中止したものも積まれ `cancelled` が読めること /
  **保存したファイルに平文の資格情報が 1 文字も無いこと**（ファイルを読んで確かめる）/
  `..` でフォルダの外を読ませないこと
- **レビュー UI の純関数**（`lib/reviewPlan.ts` / `reviewFindings.ts` / `reviewMarkdown.ts`）:
  実行できない理由の出し分け（**理由が重なったら先に直すものを出す**）/
  指摘 → 差分行の対応（当たる / **差分に無い行** / `line` が `null` / 同じ行に 2 件 /
  削除された行）/ Markdown の組み立て（**指摘 0 件** / `markdown` だけ / 失敗 / 中止 /
  **見出しに使う文字の混入**）
- **LLM**: JSON パース失敗時の Markdown フォールバック経路
- **ログ**（T-24）: 日付ごとにファイルが分かれること ／ **7 日より古いものだけ消え、
  ちょうど 7 日は残る**こと ／ **自分が作った形でないファイルに触らない**こと ／
  フォルダが無くても落ちないこと ／ **書き出した行に平文の資格情報が無い**こと
  （**伏せ字になった形まで見る** — 「含まれない」だけだと、そもそも書けていないときも通る）／
  **`log` の口からファイルまで繋がっている**こと ／ 同時に書いても行が壊れないこと
- **git の stderr のマスキング**（T-24）: **git が伏せてくれない形で試す。**
  `fetch <資格情報付き URL>` では git 自身が資格情報を落とすので、
  **マスクを外してもテストが緑のまま通る**（知らないオプションを渡すと、
  受け取った文字列がそのまま stderr に出る）
- **ショートカット**（T-25）: 修飾キーの違いで別の動作になること（`Ctrl+R` と
  `Ctrl+Shift+R`）／ **同じ押し方が 2 つの動作に当たらない**こと ／
  入力欄ではどれも当たらないこと ／ 別名（`↓` と `j`）／ `meta` 付きは全部外れること
- **設定の入力検証**（T-25）: 空欄 / 0 / 最小 / 最大 / 最大 +1 / 数字でない / 小数 /
  前後の空白 ／ **0 に意味がある欄（放置警告 = 0 で警告しない）を 0 のまま通す**こと ／
  **Rust の `normalize()` が画面と同じ範囲で締める**こと
- **差分の中の検索**（T-25）: 当たる / 当たらない / **空の検索語** / 大文字小文字 /
  1 行に複数 / 左右に並べたとき両側 / **hunk の見出しには当たらない**こと /
  **端で折り返す**こと（1 件・0 件も）
- 結合テスト用リポジトリはスクリプトで生成する（手元の実リポジトリに依存しない）。
  生成先は `%TEMP%\gitpeek-test-repos`
- フロントは**純関数だけ**テストする。**対象は `src/lib/*.test.ts` が正**（`*.ts` と対で置く）。
  コンポーネントテストはしない。フックとコンポーネントから純関数を切り出してここへ置く
  （例: 一括 fetch の進行は `lib/fetchState.ts`、実行は `hooks/useFetch.ts`）。
  **裏返すと、`.tsx` / フックに書いた判定にはテストが 1 つも当たらない。**
  出し分け・検証・整形をそこへ直書きしないこと（clone の「既定の保存先」は直書きしたせいで、
  **条件が常に成立せず一度も表示されないまま**コマンドの受け入れ条件を全部通した）

### 変更したら通すもの

```
npm run test:rust      # cargo test（単体＋結合）
npm run check:rust     # cargo clippy --all-targets -- -D warnings
npm run test           # vitest（純関数）
npm run typecheck      # tsc --noEmit
```

**速度を測るときはリリースで測ること。** `cargo test` は debug なので 4〜5 倍遅い
（50,000 コミットのレーン計算が debug 162ms / release 37ms）。

---

## 9. 実装フェーズ

**進捗とタスクの詳細は [`task_lists.md`](task_lists.md) を見ること。** 進捗の唯一の正はそちらであり、
ここには Phase の一覧だけを文脈として残す。

縦切りで進める。**各 Phase 終了時に必ず起動して触れる状態にすること。**

| Phase | 内容 |
|---|---|
| 0 | Tauri v2 雛形 ＋ git 検出 ＋ git コマンドログパネル |
| 1 | リポジトリ登録・一覧・切替 ＋ `git log` 全件取得とパース |
| 2 | レーン計算（テスト付き）＋ SVG グラフ ＋ 仮想スクロール ← **最初の判定ポイント** |
| 3 | ブランチ / タグツリー、ref チップ、表示 ON/OFF、ahead/behind |
| 4 | コミット詳細 ＋ 変更ファイル一覧 ＋ 差分表示（Shiki / side-by-side / word diff） |
| 5 | 任意 2 コミット間差分、作業ツリー差分 |
| 6 | fetch / checkout / FF マージ ＋ 各種ガード |
| 7 | clone |
| 8 | AI レビュー |
| 9 | 設定画面、テーマ、ショートカット、エラー処理の仕上げ、配布ビルド |

**Phase 2 の判定ゲート（T-08）は 2026-09-03 に合格した。** レーン割り当てと描線の決定
（DESIGN.md §5.1, §5.2）はこれで確定であり、**以降の Phase から動かさない**。

**v1 の DoD**: Phase 9 完了 ＋ 実リポジトリ 5 個以上で 1 週間実運用し、既存 GUI クライアントを
一度も開かずに済んだこと。

---

## 10. 開発環境メモ

- ビルド・実行は Windows のみ。ただし**閲覧対象には Linux 由来のソースが含まれる**前提で
  文字コード（UTF-8 / Shift_JIS / EUC-JP 自動判別）と改行コード（LF/CRLF 混在の警告）を扱う。
  **文字コードの判別は `src-tauri/src/encoding.rs` を通す**（`exec.rs` の `stdout_lossy` を
  ファイル内容に使わない）。判別は順序で決めているので **EUC-JP のひらがなは Shift_JIS と
  判定される**。直す手段は手動上書きであり、統計的推定は入れない（DESIGN.md §9.1）。
- **Rust は 1.88 以上が必須**（Tauri v2 自体は 1.77.2+。依存の `time` が edition2024 を要求して
  1.85 になり、T-20 で入れた `keyring 4.2` がさらに 1.88 を要求する）。
  ビルドが通らなくなったらまず `rustup update stable`。
- 開発時の起動はリポジトリ直下の **`GitPeek.bat`**（ダブルクリック可）。中身は
  `npm run tauri dev` で、node_modules が無ければ先に `npm install` する。
  **大きなリポジトリを触るときは `npm run start:release`**（Rust が debug だと
  レーン計算が 4〜5 倍遅い）。
- **起動直後に数十秒ウィンドウが真っ白になったら、まずフィルタリング系ソフトを疑う。**
  この環境の AdGuard は WebView2 のページに content script を差し込むため
  `injections.adguard.org` へ問い合わせに行き、**それが 29 秒返らない間ページの評価が止まる**
  （実測。HTML 自体は 33ms で届いている）。`index.html` の受け皿もこの間は動けないので、
  アプリ側からは手当てできない。AdGuard のフィルタリング対象から `msedgewebview2.exe`
  または `localhost` を外すこと。
- 配布はポータブル zip のみ。**Tauri の bundler に zip ターゲットは無い**ので、
  `src-tauri/target/release/GitPeek.exe` を npm script で zip 化する。
