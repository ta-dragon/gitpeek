# CLAUDE.md — GitPeek

個人用 Git ビューワー **GitPeek**（Tauri v2 + Rust + React/TypeScript）。本ファイルは**実装中に毎回効く
制約**だけを置く。**理由と実測値はここに無い** — なぜそう決めたかは DESIGN.md（節番号を添えてある）。

| ファイル | 担当 |
|---|---|
| 本ファイル | **何を守るか** — 破ると静かに壊れる制約 |
| [`task_lists.md`](task_lists.md) | **今やること** — タスクの詳細と進捗（進捗の唯一の正）|
| [`docs/DESIGN.md`](docs/DESIGN.md) | **なぜそう決めたか** — 全決定・決定理由・実測値。**冒頭に目次がある**。過去に踏んだ落とし穴と、承知で抱えている割り切りは **§17** |

**新しいセッションは `task_lists.md` の「現在地」節から読むこと。**
**同じことを 2 か所に書かない** — 迷ったら、この表の担当どおりの 1 か所へ置く
（2026-09-09 に 3 ファイルを整理した。DESIGN.md §17 の冒頭）。

## 1. このアプリは何をしないか

GitPeek は**ビューワー**である。以下は「未実装」ではなく「実装しないと決めた」もの（DESIGN.md §1.1）。
**提案も実装もしてはいけない。**

- あらゆるコミット操作（commit / amend / rebase / cherry-pick / revert）
- stage / unstage / discard / stash の作成・適用（作業ツリーは **read-only** 表示のみ）
- `--force` 付き checkout、自動 stash、非 fast-forward マージ（`merge` は常に `--ff-only`）
- shallow clone (`--depth`)、`--single-branch`、clone 時の `--branch`、`--shallow-submodules`
- **タグ**の作成・削除・リネーム、**ブランチの削除・リネーム**、**ブランチの自動作成**、自動更新、
  コード署名（署名は 2026-09-08 に調べ直して「しない」を維持。DESIGN.md §2.3.4）
- 外部通信（Gravatar 等）。ネットワークに出るのは git の fetch/clone と LLM API だけ

git に対して書き込むのは **checkout / fetch / merge --ff-only / clone** の 4 つだけ。**git の動詞を
増やさない** — 「取ってきて進める」（T-31）は `git pull` ではなく、この 4 つのうち fetch と
merge --ff-only を順に呼ぶだけで作ってある（DESIGN.md §8.6）。

### この一覧を変えるときは利用者に聞く

**エージェントの判断で緩めてはいけない。** 必要だと思ったら、実装せずに聞くこと。緩めたのは 3 件で、
**どれも利用者が決めた**。緩め方も揃えてある — **禁止をやめたのではなく、「黙ってやらない」ほうを残して
条件を付けた。** 同じ形が要るなら倣うこと。

| いつ | 何を | 付けた条件 |
|---|---|---|
| 2026-09-04 | ローカル追跡ブランチの作成（`checkout -b --track`）| checkout の確認画面で**選ばれたときだけ**。選ばれなければ detached HEAD（DESIGN.md §8.1）|
| 2026-09-05 | サブモジュールの取り込み（`--recurse-submodules`）| clone の確認画面で**チェックされたときだけ**。**既定 OFF で、黙って付けない**（DESIGN.md §8.4）|
| 2026-09-08 | fetch のあとに続けて `merge --ff-only` を走らせる（`git pull --ff-only` 相当）| 右クリックの「**取ってきてから … まで進める**」を選び、**確認画面で押されたときだけ**。**fetch のボタンは今までどおり取ってくるだけ**で、ブランチは進めない。進められないときは実行せず理由を出す（DESIGN.md §8.6）|

## 2. git 実行の不変条件（破ると静かに壊れる）

**すべての git 実行は `src-tauri/src/git/exec.rs` を通す。** ここ以外で `Command::new("git")` を書かない。
入口は `run`（完了を待つ）/ `run_streaming`（**stdout** を読みながら全件ダンプ）/ `run_progress`（**stderr** を
読みながら ＋ 中止できる。fetch / clone）の 3 つ。**3 つとも `build()` で下を付ける。新しい入口も `build()` を
通す**（一部だけに足すと、経路によって日本語パスが化けたり認証で固まったりする。DESIGN.md §3.1）。

```
-c core.quotepath=false  -c core.autocrlf=false  -c core.pager=cat  -c color.ui=false  -c core.commitGraph=false
GIT_TERMINAL_PROMPT=0    GIT_SSH_COMMAND=ssh -o BatchMode=yes
```

- **`git log --all` を使わない**（タグ・stash・notes を起点 ref にする）。正しくは
  **`git log --branches --remotes HEAD --topo-order`**（DESIGN.md §4.2）。**`.git/index.lock` をアプリから
  削除しない**（検出したら表示するだけ）。認証は git CLI へ完全委譲し、**パスワードもトークンも扱わない。**
- 出力パースは機械可読形式だけに依存する。**`--raw` / `--numstat` の `-z` では、リネームとコピーだけパスを
  2 つ食う**（`git/diff.rs`）。**リネームの差分は pathspec に新旧両方のパスを渡す**（新だけだと全行追加になる）。
- **コミット検索の引数は `git/search.rs` だけが組み立てる**（フロントから git のフラグを受け取らない）。
  値は `--grep=<値>` の **1 引数の形**で渡す。**`--author` を 2 つ並べない** — `--all-match` を付けても
  OR のままで、絞ったつもりが広がる（`--grep` の 2 つは `--all-match` で AND になる。DESIGN.md §6.6, §17.1）。
- **ルートコミットに `git diff` を使わない。** `diff-tree --root --no-commit-id -r`（patch は `-p`）。空ツリーの
  SHA を定数で埋め込まない（SHA-256 リポジトリで変わる）。
- ahead/behind は `rev-list --count` を呼ばず**メモリ上のグラフから計算**する。**`%s` は最初の段落を 1 行に
  潰す**ので、全文が要るところは `%B` を取り直す（`%s` ＋ `%b` で作らない）。
- **clone だけは `-C` を使わない**（`repo` は `None`、**URL と保存先は絶対パス**）。**残骸を消してよいのは
  「実行の直前に無かったと確かめられた」ときだけ**（DESIGN.md §8.4）。
- **checkout の対象は形で渡し分ける**（DESIGN.md §8.1）。**ローカルブランチは短い名前**、それ以外は**完全な
  ref 名 ＋ `--detach`**。短い名前だけだと DWIM が**ローカルブランチを勝手に作り**（§1 違反）、逆に完全な
  ref 名をローカルブランチへ渡すと detached になる。作ってよいのは `checkout -b <名前> --track <完全な ref 名>`
  だけで、**確認画面で選ばれたときにしか通らない経路**にする。**`--detach` で塞ごうとしない**（DWIM が内部で
  `-b` を立て、見当違いのエラーで落ちる）。

## 3. レーン割り当ての不変条件

`src-tauri/src/graph/lane.rs`。**このアプリの心臓部。変更したら必ずユニットテストを通すこと。** 決定は
Phase 2 の判定ゲート（T-08、2026-09-03 合格）で確定であり、**以降動かさない**（DESIGN.md §5.1）。

1. **lane 0 は幹に予約。** 既定ブランチ（`origin/HEAD` → `main`/`master` → HEAD の順）の第一親チェーンは必ず
   lane 0 を一直線に通る。**幹は既定ブランチより新しい側へも伸ばす。**
2. レーン再利用は最小空きレーン。ただし**解放直後の 2 行は保留**（`RESERVE_ROWS`）。**ルートコミットで
   終わったレーンは二度と配らない**（同じ色の線が穴を空けて続いて見える）。
3. マージコミットの第 2 親は**右側に新レーンを起こす**。**レーン数に上限を設けない**（横スクロール）。
4. コミットは**リポジトリ選択時に全件一括取得**し、レーンを一度で確定する（増分計算はしない）。**作業ツリーの
   擬似行はレーン計算の対象外**で、HEAD へ点線で接続する。
5. ブランチ表示 ON/OFF は、可視 ref から**到達可能集合を再計算して振り直す**（淡色化ではない）。

## 4. セキュリティ上の必須事項

- **秘匿情報のマスキングは `redact.rs` を全出力が通る**（画面とログの両方）。**通す場所は「その文字列を
  組み立てるところ」で 1 度だけ**にする（一覧は DESIGN.md §13.4.1）。**git の stderr は `git/exec.rs` の
  `GitOutput` 組み立てで通してある** — ここを通さない経路を作らず、**git 自身が伏せることを当てにしない。**
- **ログは `logging::format_line` を通る**（`log` の口から書けば自動）。**ファイルへ直接書かない。ログに本文を
  書かない** — プロンプト・差分・レビュー本文・skill 本文・ファイル内容は対象外で、LLM は接続先・モデル・
  所要時間・結果の種別まで。**git の stderr は全文残す。**
- **API キーの平文に触ってよいのは `secret.rs` と `llm/client.rs` だけ**（`settings.json` に書くのは参照キーのみ）。
  **通信は保存済みのプロファイルに限る**（DESIGN.md §10.2.1）。**参照キーを決めるのは
  `Store::upsert_llm_profile` だけ。プロファイルを消したら資格情報も消す。**
- **API キー欄は「空欄＝変えない」。** `keep` / `replace` / `clear` を**真偽値ではなく種類**で送る。
- **LLM から外へ出る文字列は `client.rs` の `sanitize` を通す。キーの伏せ字化は「独立した語」のときだけ**
  （`mask_key`。素の `replace` だと 1 文字のキーで応答が壊れる）。
- **OS にパスを渡して開くコマンドは、フロントからパスを受け取らない** — `open_log_folder` は Rust が持つ 1 つを
  開き、`open_repository_folder` は登録の ID を受け取ってパスは `Store::repository()` が引く。`opener:allow-open-path` を capability へ足すのも禁止。
  **開く直前に実在も確かめる。**
- **リポジトリ内 skill (`<repo>/.gitpeek/skills/*.md`) は既定で無効。信頼はファイル 1 つずつ**（`llm/skill.rs`）。
  内容が変わったら**そのファイルだけ**が未決へ戻り、**増えたファイルは記録が無いので未決のまま**
  （DESIGN.md §11.2）。**押した時点で読んでいた内容だけを信頼する**（画面が見せていたハッシュを一緒に
  受け取り、食い違ったら記録しない）。
- **プロンプトへ渡せる skill 本文が出てくるのは `SkillEntry::usable_body()` だけ**（本文のフィールドは非公開、
  **既定は「配らない」**）。画面で読ませる `preview` を **LLM へ渡してはいけない。**
- **skill は直下の `*.md` だけ読む。サブディレクトリと symlink を辿らない**（`.md` の名前で秘密鍵を指されると
  中身がプロンプトへ入る）。1 ファイル 64 KiB / 1 か所 64 件が上限。**使わない skill が同名の別の skill を
  押しのけてはいけない。どちらが効いているかは画面に出す。**
- **skill ごとの「追加の指示」は `settings.json` に持ち、skill ファイルを書き換えない**（`usable_body()` から
  本文の後ろに付いて出るので、**未信頼の skill には付かない**）。

## 5. データと設定の置き場所

置き場所と中身は **DESIGN.md §12.1**（`%APPDATA%\com.tatsu.gitpeek\` の下に `settings.json` / `state.json` /
`skills\` / `reviews\<repo-id>\` / `logs\`）。**OneDrive 配下に設定を置かない**（同期競合とファイルロックの温床）。
`settings.json` は手編集を想定、`state.json` は**壊れたら捨てて再生成できること。**

- **`settings.json` の数値の範囲は 2 か所で締める。** 画面（`src/lib/settingsForm.ts` の `NUMBER_FIELDS`）と
  Rust（`Settings::normalize()`）に**同じ値**を置く。**片方だけ変えると Rust のテストが落ちる。**
- **レビュー結果を保存するのは `start_review` の中**（`store/reviews.rs`）。フロントに保存させると**保存し忘れる
  経路**ができる。**上書きせず積み、中止したものも積む。** 形は `ReviewRun` をそのまま包んだもので、別の形へ
  変換しない（DESIGN.md §12.4）。**`baseUrl` は `redact` を通してから書く**（§4）。**壊れた 1 件で一覧を
  落とさない** — 読めないものと将来版は**理由付きで一覧に残す**。

## 6. UI の固定事項

- 日本語 UI。**全表示文言は `src/i18n/ja.ts` に集約**し、コンポーネントからはキーで参照する（i18n ライブラリは
  入れない）。**唯一の例外が `index.html` の起動時受け皿**（DESIGN.md §13.5）。
- **文言は「何が起きるか」で書く。** git の用語をそのまま出さない（「FF マージ」は通じなかった）。メニューは
  「ローカルの main を origin/x まで進める」のように**方向を名前で書き**、**できないときは何ができないのかを
  説明する**。**FF マージは「取り込む」ではなく「進める」**（合流ではなく、ローカルのブランチを相手の
  位置まで動かすだけ。こう書くと、手元にだけあるコミットがあると進められないことが言葉から分かる。
  2026-09-08 に利用者が決めた）。**コードの中は `merge` / `fetch_and_merge` のままでよい**が、
  **画面へ出す文字を書くときは「進める」**（コード側のコメントを文言へ写さないこと）。**似た項目が並ぶときは、違いを書き出しに置く**（末尾や語の途中だと、
  同じ書き出しのまま数文字しか違わず見分けられない。T-31 の目視）。
  ただし**診断に要る語（choices / fast-forward / base URL）とコマンド名は残す**。**文言はまだ途中**
  （T-29 は 1 巡目。task_lists.md「現在地」を読むこと）。
- **押せない選択肢を画面から消さない。** 押せない形で残し、**押せない理由といまの値**を出す。**判定は
  コンポーネントに直書きせず純関数へ**（§8）。**覚えている既定は必ず画面に出す** — 読めないと、勝手に
  変わっているようにしか見えない。
- **「取ってきて進める」の中止ボタンは、取ってくる間しか出さない**（`merge --ff-only` は止められない。
  段は Rust からの `fetch-merge-phase` で届く）。**進められるかどうかは取ってくる前に決めない** —
  確認画面ではいまの ahead/behind を注記として出すだけで、実行の可否は取ってきたあとの判定が決める。
- **リポジトリに紐づく設定を、アプリ全体の「設定」に混ぜない**（リポジトリ 1 つぶんは右クリック →
  「このリポジトリの設定」）。**アプリ全体の設定の入口は 1 つだけ**（ヘッダの「設定」と `Ctrl+,`）。
- レイアウト・寸法・色・スクロールの細部は **DESIGN.md §5.2 / §6.1 / §6.3 / §6.4** が正。とくに次は破ると静かに
  ずれる — **コミット詳細と変更ファイル一覧を差分本体の上に積まない** / **列見出しは行と同じスクロール
  コンテナに入れて縦だけ sticky** / **行の中の SVG は `display: block`** / **非コミット行はグラフの窓の置き場所
  だけ下げる**（viewBox にも足すと打ち消し合う）/ **行は `LaneLayout.rows` を順に辿って作る** / **伸びる
  スクロールペインの `flex-basis` は 0** / **ダイアログの中のボタンは折り返す** / **`.button--primary` に共通の
  `.button:hover` を効かせない** / **仮想スクロールに渡す ref は、スクロールする要素にだけ付ける**
  （フォーカス用と共有すると外枠が勝ち、描く行が先頭で固まる。DESIGN.md §17.1）。
- **AI レビューのドロワーはコミット情報の列と入れ替える**（並べない。幅は `paneRatios.reviewDrawerWidth`）。
  **実行前パネルはモーダルにしない。指摘のバッジは差分に実際にある行にだけ出し**、当たらなかったものは
  **ドロワーに残して当たらなかったと明記する**（`lib/reviewFindings.ts`）。
- **キーの形は `src/lib/shortcuts.ts` の対応表だけが持つ。** 判定は `matches(event, action)` を通し、`.tsx` や
  フックに `event.key === ...` を書かない。**修飾キーは「書いていないものは押されていない」ことまで見る。**
  `Esc` は `hooks/useEscape.ts`。**差分の中の検索（`Ctrl+F`）は文字を塗らない**（3 つ目の色は読めない）。
- **タグにチェックボックスを置かない**（起点 ref ではないので外してもグラフが変わらない）。**git コマンドログ
  パネルは常設**（直近 500 件、リポジトリ切替でクリアしない）。**出し入れの口は設定の「一般」1 か所だけ**で、
  状態は `settings.json` の `ui.showCommandLog` に残る。**ヘッダのボタンを復活させない。**

## 7. AI レビューの固定事項

理由と細部は DESIGN.md §10。ここに残すのは、外に書き直すと壊れるものだけ。

- **OpenAI 互換 API 1 系統のみ**（Ollama も `http://localhost:11434/v1`）。**HTTP は `llm/client.rs` に閉じる** —
  接続・タイムアウト・失敗の言い分け・マスキング・SSE（`chat_stream`）・400 の見立てが全部ここで、`ureq` を直に触らない。
  `Authorization` は**キーが空でないときだけ**付け、base URL は末尾の `/` を落として繋ぐ。
- **接続テストは `/v1/models` で済ませず** `chat/completions` に 1 往復させる（models が通っても生成まで通るとは
  限らない）。**失敗は「見出し ＋ 接続先の言い分 ＋ 展開で生の応答」の 3 段**で、整形は `src/lib/llmMessage.ts`。
- **レビューの計画と実行は `llm/review.rs` に閉じる。同じ見積もり・分割数をフロントに置かない**（必ずずれる）。
  **差分本文は `FileDiff` の hunks から組み立てる**（別経路で取り直すと Shift_JIS のファイルで JSON が壊れる）。
- **流す差分は末尾を持ち越してからマスクする**（`Masker`）。**幅は伏せたい形が収まり、かつ実際に来る応答より
  短いこと**を突き合わせて決める（長すぎると差分が 1 度も流れない）。**鍵が幅より長ければ鍵の長さまで広げる。**
- **`response_format` の `json_object` は送る**（Ollama は `format: "json"`）。**咎められたら外して 1 度だけ投げ直し、
  以後その実行では付けない。プロンプトでの JSON 要求は外さない。** 投げ直すのは**見立てが立つときだけ**。
- **JSON スキーマを要求し、パース失敗時は生出力を Markdown として出すフォールバックが必須**（ローカル小型
  モデルは守れない）。**この経路はテストで必ず通す。**
- モデルに渡すのは **パス ＋ unified diff (`-U10`) ＋ コミットメッセージ ＋ skill 本文**のみ。**ファイル全文は渡さない。**
  skill は `SkillEntry::active()` で絞り、本文は `usable_body()` からしか取らない（§4）。**コミットメッセージを
  渡すのは「1 つのコミットとその親」のときだけ**（親子かは `diff::is_parent_of` で git に聞く）。
- 投入単位は**ファイル単位＋最後に全体サマリ**（超過時のみ hunk 分割）。実行は既定逐次（並列度 1、最大 3）で、
  **1 ファイルの失敗で全体を止めない。**

## 8. テスト

**何を担保するかの一覧は DESIGN.md §14.6**（レーン / パーサ / 文字コード / 差分 / fetch / checkout / clone /
資格情報 / skill / レビュー / SSE / ログ）。戦略は DESIGN.md §14。毎回効くのは次の 4 つ。

- **フロントは純関数だけテストする**（`src/lib/*.test.ts` が正。コンポーネントテストはしない）。**裏返すと、
  `.tsx` / フックに書いた判定にはテストが 1 つも当たらない。** 出し分け・入力検証・整形は `src/lib/*.ts` へ。
- **フロントから送る構造体は、Rust 側が実際にその JSON を食えることを固定する**（組み立てだけ固定しても、
  受け取りの形が違えばコマンドは 1 度も走らない）。
- **メッセージの文言そのものを assert しない**（T-29 で 4 本落ちた）。直すたびに落ちると、直すのが億劫になって
  古い文章が残る。見るのは意図のほう。**ただし落ちたテストが正しいこともある。**
- **相手が親切だと、テストは何も確かめないまま緑になる。** マスキングは**伏せてくれない形**で試し、**マスクを
  外すと落ちることを 1 度確かめる。**

**変更したら次の 4 つを通す。速度を測るときはリリースで**（`cargo test` は debug で 4〜5 倍遅い）。

```
npm run test:rust   # cargo test（単体＋結合）    npm run test        # vitest（純関数）
npm run check:rust  # cargo clippy -D warnings    npm run typecheck   # tsc --noEmit
```

## 9. 実装フェーズと DoD

**進捗とタスクの詳細は [`task_lists.md`](task_lists.md) が唯一の正。** Phase の一覧と判定ゲートは DESIGN.md §15。
縦切りで進め、**各 Phase 終了時に必ず起動して触れる状態にすること。** **v1 の DoD** は、Phase 9 完了 ＋
実リポジトリ 5 個以上で 1 週間実運用し、既存 GUI クライアントを一度も開かずに済んだこと。

## 10. 開発環境メモ

- ビルド・実行は Windows のみ。ただし**閲覧対象には Linux 由来のソースが含まれる**前提で文字コードと改行を
  扱い、**判別は `src-tauri/src/encoding.rs` を通す**（`exec.rs` の `stdout_lossy` をファイル内容に使わない）。
  **EUC-JP のひらがなは Shift_JIS と判定される** — 直す手段は手動上書きで、統計的推定は入れない（DESIGN.md §9.1）。
- **Rust は 1.88 以上が必須**（Tauri v2 自体は 1.77.2+。依存の `time` が 1.85 を、`keyring 4.2` が 1.88 を要求する）。
  ビルドが通らなくなったらまず `rustup update stable`。
- 開発時の起動はリポジトリ直下の **`GitPeek.bat`**（中身は `npm run tauri dev`。node_modules が無ければ先に
  `npm install`）。**大きなリポジトリを触るときは `npm run start:release`**（debug はレーン計算が 4〜5 倍遅い）。
- **起動直後に数十秒ウィンドウが真っ白になったら、まずフィルタリング系ソフトを疑う。** この環境の AdGuard は
  WebView2 のページに content script を差し込むため `injections.adguard.org` へ問い合わせに行き、**それが
  29 秒返らない間ページの評価が止まる**（実測。HTML 自体は 33ms で届いている）。`index.html` の受け皿もこの間は
  動けないので、**アプリ側からは手当てできない。** AdGuard のフィルタリング対象から `msedgewebview2.exe`
  または `localhost` を外すこと。
- **リリースは手元で作って上げる**（`npm run release` → `scripts/release.ps1`。GitLab.com に Windows の
  共有ランナーが無いため。DESIGN.md §2.3.2）。**トークンは環境変数 `GITLAB_TOKEN` から読むだけ**で、
  スクリプトにもリポジトリにも置かない。
- **GitLab が本流、GitHub (`ta-dragon/gitpeek`) はバックアップ**（`npm run backup:github`）。
  **GitHub へはビルドし直さず、本流へ上げた zip を SHA-256 で突き合わせてから同じものを写す。**
  トークンは `GITHUB_TOKEN` から読むだけで、**remote URL にも `.git/config` にも書かない。**
  **git への push は Basic**（Bearer は API 専用で、push だけ 401 になる。DESIGN.md §2.3.2）。
- **`.ps1` は UTF-8 BOM 付きで保存する**（`release.ps1` も `backup-github.ps1` も）— Windows PowerShell 5.1 は
  BOM の無い `.ps1` を CP932 として読み、日本語のコメントが閉じ引用符を飲み込んで構文エラーになる。
- **アイコンを変えたら `npm run tauri icon docs/images/app-icon.png` を実行し、`src-tauri/build.rs` を
  触ってから起動する**（元画像は 1024×1024 の透過 PNG）。**cargo は `src-tauri/icons/` を入力として
  見ていない**ので、touch しないと `tauri dev` が「変更なし」と判断し、**古いアイコンの exe がそのまま
  起動する**（2026-09-08 に踏んだ。icon.ico は新しいのに画面だけ古い、という形で出る）。
  `package:zip` は先に touch を走らせるので、配布ぶんは気にしなくてよい。**`icons/android` と
  `icons/ios` は使わないので追跡しない**（`.gitignore` に入れてある）。
- 配布はポータブル zip のみ（`npm run package:zip`。Tauri の bundler に zip ターゲットは無いので自前で固める）。
  **`tauri.conf.json` の `mainBinaryName` を消さない** — 消すと実体が `gitpeek.exe`（小文字）になるが、Windows は
  大文字小文字を区別しないので **`package:zip` は偶然通り、壊れたことに気付けない**（DESIGN.md §2.3）。
